// Cheating presets loading
import supergreenkitx4 from '../presets/supergreenkitx4.json'
import supergreenkitx6 from '../presets/supergreenkitx6.json'

export const state = () => ({
  configs: [
    supergreenkitx4,
    supergreenkitx6,
  ],
})
